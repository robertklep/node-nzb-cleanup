#!/usr/bin/env node

let clean = require('..');

if (process.argv.length < 3) {
  console.error('Use: %s NZBNAME [NZBNAME ...]', process.argv[1].replace(/.*\//, ''));
  process.exit(1);
}

process.argv.slice(2).forEach(name => console.log( clean(name) ));
